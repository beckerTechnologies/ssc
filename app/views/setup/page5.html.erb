<h1>Setup</h1>
<p>Congratulations ! setup process is complete. Press the button below to send the challange text. </p>

<% if flash[:success].present?  %>
  <script>alert("SMS has been sent")</script>
<% end %>
<%= form_tag :url => "./page5" do |p| %>
  <p>
  <%= hidden_field_tag 'send', 'yes'  %>
  <%= submit_tag('Send Challange text on email/sms') %>
  </p>
<% end %>
(developers comment: after the text is sent, a popup box will show up containing the text "Congratulaitons! use your ssc to file ur taxes etc". with single acknoldgement button which when pressed will redirect to home page. )
<%= form_tag(root_path, method: "get") do %>
  <%= submit_tag("back to Home") %>
<% end %>
